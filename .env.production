# Default production-mode build for this repo is still the public demo.
# Never point this file at https://api.advisortrack.co.za — that is the production backend.
# Public demo deploys should use `vite build --mode demo` (.env.demo).
VITE_API_BASE_URL=/api/v1
VITE_ADVISORTRACK_MODE=demo
